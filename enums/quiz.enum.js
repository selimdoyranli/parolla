export const quizTypeEnum = Object.freeze({
  QA: 'qa',
  CHOICES: 'choices'
})

export const questionTypeEnum = Object.freeze({
  TEXT: 'text',
  MEDIA: 'media'
})

export const answerTypeEnum = Object.freeze({
  TEXT_FIELD: 'textField',
  TRIVIA: 'trivia'
})

export const choiceTypeEnum = Object.freeze({
  TEXT: 'text',
  MEDIA: 'media',
  YOUTUBE: 'youtube'
})
