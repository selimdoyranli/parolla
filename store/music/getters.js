export default {
  isGameOver(state) {
    return state.isGameOver
  },

  result(state) {
    return state.result
  },

  dialog(state) {
    return state.dialog
  },

  dailyPlayingCount(state) {
    return state.dailyPlayingCount
  }
}
