export default {
  targetWord(state) {
    return state.targetWord
  },

  isGameOver(state) {
    return state.isGameOver
  },

  result(state) {
    return state.result
  },

  dialog(state) {
    return state.dialog
  }
}
