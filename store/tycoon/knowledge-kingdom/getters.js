export default {
  gold(state) {
    return state.gold
  },

  totalGold(state) {
    return state.totalGold
  },

  goldPerSecond(state) {
    return state.goldPerSecond
  },

  goldPerClick(state) {
    return state.goldPerClick
  },

  ownedItems(state) {
    return state.ownedItems
  },

  items(state) {
    return state.items
  },

  isLoaded(state) {
    return state.isLoaded
  }
}
